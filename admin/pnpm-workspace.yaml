# Single-package workspace (root only). Allow native/postinstall builds needed by Angular.
onlyBuiltDependencies:
  - esbuild
  - '@parcel/watcher'
  - lmdb
  - msgpackr-extract

allowBuilds:
  esbuild: true
  '@parcel/watcher': true
  lmdb: true
  msgpackr-extract: true
